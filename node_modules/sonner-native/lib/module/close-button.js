"use strict";

import * as React from 'react';
import { Pressable, Text } from 'react-native';
import { X } from "./icons.js";
import { jsx as _jsx } from "react/jsx-runtime";
export const CloseButton = ({
  dismissible,
  close,
  closeButton,
  onDismiss,
  id,
  closeButtonStyle,
  defaultStyles,
  closeButtonIconStyle
}) => {
  if (!dismissible) {
    return null;
  }
  if (close) {
    // `close` typechecks as a bare string/number, which crashes React Native
    // when rendered directly inside a View.
    return typeof close === 'string' || typeof close === 'number' ? /*#__PURE__*/_jsx(Text, {
      children: close
    }) : close;
  }
  if (closeButton) {
    return /*#__PURE__*/_jsx(Pressable, {
      onPress: () => onDismiss?.(id),
      hitSlop: 10,
      style: closeButtonStyle,
      children: /*#__PURE__*/_jsx(X, {
        size: 20,
        color: defaultStyles.closeButtonColor,
        style: closeButtonIconStyle
      })
    });
  }
  return null;
};
//# sourceMappingURL=close-button.js.map