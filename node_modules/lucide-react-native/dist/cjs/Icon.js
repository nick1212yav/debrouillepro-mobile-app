/**
 * @license lucide-react-native v1.45.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */

'use strict';

var react = require('react');
var NativeSvg = require('react-native-svg');
var mergeClasses = require('./shared/src/utils/mergeClasses.js');
var buildLucideIconForReact = require('./shared/src/build/buildLucideIconForReact.js');
var hasA11yProp = require('./shared/src/utils/hasA11yProp.js');
var defaultAttributes = require('./defaultAttributes.js');
var context = require('./context.js');

function _interopNamespaceDefault(e) {
  var n = Object.create(null);
  if (e) {
    Object.keys(e).forEach(function (k) {
      if (k !== 'default') {
        var d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: function () { return e[k]; }
        });
      }
    });
  }
  n.default = e;
  return Object.freeze(n);
}

var NativeSvg__namespace = /*#__PURE__*/_interopNamespaceDefault(NativeSvg);

const toNativeSvgAttrName = (attributeName) => {
  if (attributeName === "class") {
    return "className";
  }
  if (attributeName.startsWith("data-") || attributeName.startsWith("aria-")) {
    return attributeName;
  }
  return attributeName.replace(/-([a-z])/g, (_, character) => character.toUpperCase());
};
const toNativeSvgAttributes = (attributes) => {
  return Object.entries(attributes).reduce(
    (acc, [attributeName, value]) => {
      if (value === void 0) {
        return acc;
      }
      acc[toNativeSvgAttrName(attributeName)] = value;
      return acc;
    },
    {}
  );
};
const Icon = react.forwardRef(
  ({
    color,
    size,
    width = size,
    height = size,
    strokeWidth,
    absoluteStrokeWidth,
    nonScalingStroke,
    children,
    iconNode = [],
    icon = {
      node: iconNode,
      size: 24
    },
    className,
    testID,
    ...rest
  }, ref) => {
    const {
      size: contextSize = 24,
      strokeWidth: contextStrokeWidth = 2,
      absoluteStrokeWidth: contextAbsoluteStrokeWidth = false,
      nonScalingStroke: contextNonScalingStroke = false,
      color: contextColor = "currentColor",
      className: contextClass = ""
    } = context.useLucideContext() ?? {};
    const hasAccessibleProp = Boolean(children) || hasA11yProp.hasA11yProp(rest);
    const [, svgAttributes, builtIconNode = []] = buildLucideIconForReact(icon, {
      color: color ?? contextColor,
      width: width ?? size ?? contextSize,
      height: height ?? size ?? contextSize,
      strokeWidth: strokeWidth ?? contextStrokeWidth,
      absoluteStrokeWidth: absoluteStrokeWidth ?? contextAbsoluteStrokeWidth,
      nonScalingStroke: nonScalingStroke ?? contextNonScalingStroke,
      className: mergeClasses.mergeClasses(contextClass, className),
      hasA11yProp: hasAccessibleProp,
      attributes: rest
    });
    const customAttrs = {
      stroke: svgAttributes["stroke"],
      strokeWidth: svgAttributes["strokeWidth"],
      ...rest
    };
    return react.createElement(
      NativeSvg__namespace.Svg,
      {
        ref,
        ...toNativeSvgAttributes(svgAttributes),
        "data-testid": testID,
        ...rest
      },
      [
        ...builtIconNode.map(([tag, attrs]) => {
          const upperCasedTag = tag.charAt(0).toUpperCase() + tag.slice(1);
          return react.createElement(
            NativeSvg__namespace[upperCasedTag],
            toNativeSvgAttributes({
              ...defaultAttributes.childDefaultAttributes,
              ...customAttrs,
              ...attrs
            })
          );
        }),
        ...(Array.isArray(children) ? children : [children]) || []
      ]
    );
  }
);

module.exports = Icon;
//# sourceMappingURL=Icon.js.map
