/**
 * @license lucide-react-native v1.45.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */

'use strict';

var react = require('react');
var toLucideIconData = require('./shared/src/utils/toLucideIconData.js');
var toPascalCase = require('./shared/src/utils/toPascalCase.js');
var Icon = require('./Icon.js');

function createLucideIcon(iconDataOrName, iconNode, aliases = []) {
  const iconData = typeof iconDataOrName === "string" ? toLucideIconData.toLucideIconData(iconDataOrName, iconNode, aliases) : iconDataOrName;
  const Component = react.forwardRef(
    (props, ref) => react.createElement(Icon, {
      ref,
      icon: iconData,
      ...props
    })
  );
  if (iconData.name) {
    Component.displayName = toPascalCase.toPascalCase(iconData.name);
  }
  return Component;
}

module.exports = createLucideIcon;
//# sourceMappingURL=createLucideIcon.js.map
